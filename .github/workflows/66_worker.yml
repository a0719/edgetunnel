     name: 混淆Worker JS文件

on:
  push:
    branches:
      - main
    paths:
      - '_worker.js'

jobs:
  obfuscate:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出仓库
      uses: actions/checkout@v4
      
    - name: 设置Node.js环境
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: 安装混淆工具
      run: npm install -g javascript-obfuscator
        
    - name: 检查_worker.js是否存在
      id: check_files
      run: |
        if [ -f "_worker.js" ]; then
          echo "File _worker.js exists"
          echo "exists=true" >> $GITHUB_OUTPUT
        else
          echo "File _worker.js does not exist"
          echo "exists=false" >> $GITHUB_OUTPUT
        fi
        
    - name: 混淆_worker.js到1hx_worker.js
      if: steps.check_files.outputs.exists == 'true'
      run: |
        javascript-obfuscator _worker.js \
          --compact true \
          --control-flow-flattening true \
          --control-flow-flattening-threshold 0.75 \
          --dead-code-injection true \
          --dead-code-injection-threshold 0.4 \
          --identifier-names-generator hexadecimal \
          --rename-globals true \
          --string-array true \
          --string-array-encoding 'rc4' \
          --string-array-threshold 0.75 \
          --transform-object-keys true \
          --unicode-escape-sequence true \
          --self-defending true \
          --debug-protection true \
          --output 1hx_worker.js
          
        echo "Successfully obfuscated _worker.js to 1hx_worker.js"
        
    - name: 提交更改
      if: steps.check_files.outputs.exists == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add 1hx_worker.js
        if [[ -n $(git status --porcelain) ]]; then
          git commit -m "Add obfuscated worker file 1hx_worker.js [skip ci]"
          git push
        else
          echo "No changes to commit"
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}